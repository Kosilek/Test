using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class Damage : MonoBehaviour
{
  /*  public UnityEvent<float, float, float> OnTakeDamage = new UnityEvent<float, float, float>();

    private void Start()
    {
        OnTakeDamage.AddListener(TakeDamage);
    }

    public void SendTakeDamage(float hp, float fill, float damage)
    {
        OnTakeDamage.Invoke(hp, fill, damage);
    }

    public void TakeDamage (float hp, float fill, float dmg)
    {
        hp -= dmg;
        fill = (hp / 100);
    }*/
}
